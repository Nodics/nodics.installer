# Nodics Installer Contracts

This folder is reserved for maintained AI and developer contracts that apply
only to `nodics.installer`.

Use root `AGENTS.md` and root `README.md` first. Add a contract here only when
installer behavior needs a durable local rule that is more specific than the
repository-wide guidance.

